{
  "name": "onecode",
  "version": "0.2.0",
  "description": "OneCode — AI 原生 IDE，以 Claude Code 为内核的容器化开发环境",
  "private": true,
  "scripts": {
    "dev": "node agent-runtime/gateway/index.js",
    "build": "echo \"No build step needed — gateway runs directly\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:yiyan-yixing/onecode.git"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
